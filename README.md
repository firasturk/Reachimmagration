# Reach Immigration — الموقع

موقع ثنائي اللغة (`/en/` و `/ar/`) مبني بـ **Astro + TypeScript**: صفحات HTML ثابتة، وJavaScript بس وين لازم.

## التشغيل
```bash
npm install
npm run dev        # http://localhost:4321/en/
npm run build      # الناتج بمجلد dist/
```
## الرفع (GitHub ثم Vercel)
1. GitHub: مستودع جديد فاضي، وبعدين **uploading an existing file** واسحب كل محتويات هالمجلد (بدون `node_modules` و `dist`) واعمل Commit.
2. Vercel: **Add New → Project → Import** للمستودع، وبعدين **Deploy**. ما في إعدادات: `vercel.json` محدد Astro، و`package.json` محدد Node 22 (Astro 7 بده 22.12 أو أحدث).
3. بعد أول رفع: حط `CRM_WEBHOOK_URL` بـ Settings → Environment Variables عشان الفورم يبعت، واربط الدومين.

أي push جديد على GitHub بينشر لحاله.

## وين كل شي
| المسار | شو فيه |
|---|---|
| `src/i18n/ui.ts` | **كل نصوص الموقع** بالعربي والإنجليزي. أي تعديل نص من هون. |
| `src/data/site.ts` | البرامج العشرين، الفروع الـ 17، الجنسيات، التراخيص، السوشال، المقالات، وقصص العملاء (فاضية). |
| `src/components/Hero.astro` + `src/styles/hero.css` + `src/scripts/hero.ts` | الهيرو (ثيم 04) زي ما انعتمد بالمعاينة: ديسكتوب + موبايل + باترن الماوس. **لا تعدّل الأرقام بالعين.** |
| `src/components/SmartMap.astro` + `src/scripts/map.ts` | الخريطة الذكية: كرة أرضية (d3-geo على canvas) مربوطة بكاروسيل البلدان. |
| `src/components/SmartPassport.astro` + `src/scripts/passport.ts` | الجواز الذكي: كتاب CSS 3D، كل صفحتين برنامج. بالعربي بينفتح من اليمين. |
| `src/components/Consult.astro` + `src/scripts/form.ts` | الفورم الذكي (4 خطوات). |
| `api/lead.ts` | دالة Vercel بتستقبل الفورم وبتحوّله للـ CRM. |
| `src/components/Film.astro`، `Why.astro`، `Routes.astro`، `Stories.astro` | الأقسام اللي فيها فيديو وصور. الفيديوهات بـ `public/video/` والصور بـ `public/images/sections/`، وكلها مقصوصة من فيديوهات العميل نفسها. |
| `src/scripts/reveal.ts` | حركة النصوص: العناوين بماسكات (طلوع أو دخول من الجنب)، والفقرات بتنقسم لسطورها الحقيقية وكل سطر بيطلع لحاله. |
| `src/scripts/media.ts` | فيديوهات الأقسام ما بتتحمّل إلا لما تقرّب من الشاشة، وبتشتغل بس وهي ظاهرة. |
| `src/layouts/Base.astro` | الـ SEO: العنوان، الوصف، canonical، hreflang، Open Graph، JSON-LD. |
| `tools/preview.mjs` | أداة معاينة بس (ملف HTML واحد). مش جزء من الموقع. |

## لازم ينعمل قبل الإطلاق
1. **الفورم:** حط `CRM_WEBHOOK_URL` بإعدادات Vercel. بدونه الدالة بترجع 501 عن قصد.
2. **اللوجو:** هلأ كلمة REACH مكتوبة. بدنا SVG الأبيض والغامق، وكود الخمري الرسمي (`--brand-accent` بـ `src/styles/global.css`).
3. **محتوى صفحات البرامج:** القالب جاهز (`src/pages/[lang]/programs/[slug].astro`) بس ما فيه مبالغ ولا مدد ولا شروط، لأنها لازم تيجي من العميل.
4. **قصص العملاء:** `stories` بـ `src/data/site.ts` فاضية. بتنحط حرفياً من نصوص العميل المعتمدة.
5. **تصنيف البرامج** (جنسية / إقامة) مأخوذ من روابط موقعهم الحالي، ولازم العميل يأكده. كمان الأرقام: +25 سنة، 20 وجهة، 17 فرع، 6 تراخيص، 5.0 على Google.
6. روابط سياسة الخصوصية والشروط بالفوتر (`#` حالياً)، ورقم واتساب مباشر بدل رابط القناة.
7. **صور البلدان:** كروت الخريطة وصفحات البرامج بدون صور خاصة بكل بلد (ما في عندنا صور حقيقية إلها). لما تتوفر بتنحط بـ `public/images/programs/<slug>.webp`.
8. الفيديوهات 720p. للشاشات الكبيرة الأحسن نسخ 1080p بنفس الأسماء بـ `public/video/`.

## الألوان
كلها توكنز بأول `src/styles/global.css`:
- **الخمري مهدّى:** نفس درجة لون الهيرو بس مخلوطة برمادي الثيم (تشبّع أقل بحوالي الربع)، فبتقرأ كنبيذ معتّق مش دموي. الدرجات: `--wine-900` · `--wine-700` · `--wine-600` (الأزرار) · `--wine-500` (النصوص والوسوم) · `--wine-400` · `--wine-300` · `--wine-100` و `--wine-050`. الخمري الأصلي الحاد محفوظ بـ `--wine-pure` للمسات الصغيرة إذا احتجناه.
- **رمادي الثيم:** `--slate-700` (خلفية ثيم 04) · `--slate-500` · `--slate-300` · `--slate-100`، مع `--mist` (أرضية الجواز) و `--ivory`.
- **درجات الأقسام** بكلاس واحد: `tone-ivory` / `tone-mist` / `tone-deep`. تبديل درجة قسم = تبديل الكلاس.
- ألوان الكرة الأرضية بتنقرأ من نفس التوكنز بـ `src/scripts/map.ts`، فما بتنفصل عن الموقع.

## ملاحظات
- كاروسيل الخريطة بيقلب لحاله كل 4.2 ثانية، وبيوقف نهائياً أول ما الزائر يلمس أو يسحب أو يكبس (مع `prefers-reduced-motion` ما بيشتغل أصلاً). الدوّارة الصغيرة جنب `Drag to rotate` بتبيّن الوقت المتبقي.
- الحركة: نفس الـ spring تبع الهيرو (`--spring`) لكل الموقع، ومع `prefers-reduced-motion` بتنطفي.
- بدون JavaScript: المحتوى كله ظاهر والفورم بيشتغل كصفحة وحدة.
- الفحص انعمل على Chrome (ديسكتوب ومحاكاة موبايل). لسا ما انفحص على Safari/iPhone حقيقي.
